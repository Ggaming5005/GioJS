# giojs-win32-x64

Pre-built `giojs-server` binary for Windows x64. Installed automatically by npm when you run `npm install giojs` on a Windows x64 machine.

Do not install this package directly — install `giojs` instead.
