# giojs-darwin-arm64

Pre-built `giojs-server` binary for macOS arm64 (Apple Silicon). Installed automatically by npm when you run `npm install giojs` on an Apple Silicon Mac.

Do not install this package directly — install `giojs` instead.
