# giojs-darwin-x64

Pre-built `giojs-server` binary for macOS x64. Installed automatically by npm when you run `npm install giojs` on a macOS x64 machine.

Do not install this package directly — install `giojs` instead.
