# giojs-linux-x64-musl

Pre-built `giojs-server` binary for Linux x64 on musl libc (Alpine and other
musl-based distributions). Installed automatically by npm when you run
`npm install giojs` on a matching machine.

Do not install this package directly - install `giojs` instead.
