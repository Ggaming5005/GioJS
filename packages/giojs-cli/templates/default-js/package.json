{
  "name": "{{PROJECT_NAME}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_ENV=development giojs-server",
    "build": "node -e \"console.log('GioJS server apps have no build step: routes and client bundles are built at server startup.')\"",
    "start": "giojs-server"
  },
  "dependencies": {
    "@gio.js/server": "^0.1.0-beta.6",
    "@gio.js/react": "^0.1.0-beta.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
